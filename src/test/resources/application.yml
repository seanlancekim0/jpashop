spring:
# 아무것도 없으면 SpringBoot가 default로 memory 모드를 지원!!
#  datasource:
##    test할 때 memory모드로 h2 DB를 사용할 수 있음
#    url: jdbc:h2:mem:test
#    username: sean
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        # show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  쿼리 파라미터 로그 남기기
  org.hibernate.orm.jdbc.bind: trace #스프링 부트 3.x, hibernate6
#  org.hibernate.type: trace #스프링 부트 2.x, hibernate5
